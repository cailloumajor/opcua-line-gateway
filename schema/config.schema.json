{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "LineGatewayConfig",
  "description": "OPC-UA line gateway configuration.",
  "type": "object",
  "properties": {
    "application_uri": {
      "description": "The globally unique identifier for the application instance, as of OPC-UA.",
      "type": "string"
    },
    "opcua_servers": {
      "description": "Mapping of machine identifier to corresponding OPC-UA server configuration.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/OpcUaServerConfig"
      }
    },
    "pki_dir": {
      "description": "The root directory of the OPC-UA PKI.",
      "type": "string"
    }
  },
  "required": [
    "application_uri",
    "pki_dir",
    "opcua_servers"
  ],
  "x-tombi-string-formats": [
    "uri"
  ],
  "$defs": {
    "MessageSecurityMode": {
      "description": "A copy of [`opcua::crypto::MessageSecurityMode`] to allow using serde remote functionality.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "None",
            "Sign",
            "SignAndEncrypt"
          ]
        }
      ]
    },
    "OpcUaServerConfig": {
      "description": "The configuration for an OPC-UA server to communicate with.",
      "type": "object",
      "properties": {
        "password": {
          "description": "The password to use if using username/password authentication.",
          "type": [
            "string",
            "null"
          ]
        },
        "security_mode": {
          "description": "OPC-UA security mode.",
          "$ref": "#/$defs/MessageSecurityMode"
        },
        "security_policy": {
          "description": "OPC_UA security policy.",
          "$ref": "#/$defs/SecurityPolicy"
        },
        "url": {
          "description": "OPC-UA server URL.",
          "type": "string",
          "format": "uri"
        },
        "user": {
          "description": "The username if authenticating to the OPC-UA server with username/password.\nIf not provided, anonymous authentication will be used.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "url",
        "security_policy",
        "security_mode"
      ]
    },
    "SecurityPolicy": {
      "description": "A copy of [`opcua::crypto::SecurityPolicy`] to allow using serde remote functionality.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "None",
            "Aes128Sha256RsaOaep",
            "Basic256Sha256",
            "Aes256Sha256RsaPss",
            "Basic128Rsa15",
            "Basic256"
          ]
        }
      ]
    }
  }
}
